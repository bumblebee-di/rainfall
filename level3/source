#include <stdio.h>

int m;

void v() {

    char str[520];

    fgets(str, 512 , stdin);
    printf(str);
    if (m == 64) {
        fwrite("Wait what?!\n", 12, 1, stdout);
        system("/bin/sh");
    }
    return;
}

int main() {
    v();
}